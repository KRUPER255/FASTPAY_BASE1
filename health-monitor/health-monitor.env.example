# Health Monitor Configuration
# Copy to health-monitor.env and fill in values

# Telegram (required for notifications)
TELEGRAM_BOT_TOKEN=your-bot-token-from-botfather
TELEGRAM_CHAT_IDS=123456789,987654321

# Optional: Throttle alerts (minutes) - avoid spam when service is down
HEALTH_THROTTLE_MINUTES=15

# Optional: Custom URLs to check (space-separated)
# HEALTH_URLS="https://fastpaygaming.com/ https://api.fastpaygaming.com/api/"

# Optional: Digest report (15-min) - max log lines per Docker source before truncation
# DIGEST_MAX_LOG_LINES=30
